<nav class="navbar navbar-expand-lg <%= admin ? 'navbar-dark bg-dark' : 'navbar-light bg-light-blue' %>">
  <div class="container">
    <%= link_to 'TestGuru', root_path, class: 'navbar-brand' %>
    <%= link_to_admin_panel %>

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarToggler" aria-controls="navbarToggler" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarToggler">
      <ul class="navbar-nav ml-auto mt-2 mt-lg-0">
        <% if user_signed_in? %>
          <li class="nav-item"><span class="nav-link"><%= t('nav.welcome', email: current_user.email).html_safe %></span></li>
          <li class="nav-item"><%= link_to t('nav.profile'), edit_user_registration_path, class: 'nav-link mr-2' %></li>
          <li class="nav-item"><%= link_to t('nav.logout'), destroy_user_session_path, method: :delete, class: 'btn btn-outline-info', role: 'button' %></li>
        <% else %>
          <li class="nav-item"><%= link_to t('nav.login'), new_user_session_path, class: 'btn btn-outline-dark', role: 'button' %></li>
          <li class="nav-item"><%= link_to t('nav.signup'), new_user_registration_path, class: 'nav-link' %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
