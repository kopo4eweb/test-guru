<%= form_with model: [:admin, @question, @answer], local: true do |form| %>
  <%= render 'shared/errors', resource: @answer %>

  <p>
    <%= form.label :title %><br>
    <%= form.text_area :title, rows: 5, cols: 70 %>
  </p>
  <p>
    <%= form.label :correct %>:
    <%= form.check_box :correct %>
  </p>
  <p>
    <%= form.submit%>
  </p>
<% end %>
